/* Any bootstap logic needed for all tests can live here */

const chai = require('chai');

chai.use(require('chai-as-promised'));

before(async () => {
    // Any bootstrapping that should be done before ALL tests here
});

after(async () => {
    // Any cleanup that should be done after ALL tests here
});
